# How it works illustration

Generated with the built-in image-generation tool using `agent-farm-banner.png` as the visual reference. Output: `how-it-works-pixel-farm.png`.

## Prompt

Use case: infographic-diagram
Asset type: accurate illustrated "How it works" flow for the Agent Farm GitHub README, wide landscape 2:1, high resolution.
Input image: the approved banner is a STYLE REFERENCE ONLY. Match its crisp pixel-art game world, midnight charcoal, warm cream text, moss green, muted gold and lilac, tiny straw-hatted cream robot farmer, wooden signs, terminal huts, grass and cozy lanterns. Do not repeat its title or scene. Technical readability takes priority over decorative detail.
Create an exceptionally clear left-to-right workflow with FOUR equal main panels in one horizontal row, each with a small pixel-art vignette and spacious dark signboard for high-contrast readable text. Connect consecutive panels with three unmistakable cream right-pointing arrows. Above panel 2, arrange three smaller input tiles with individual lines that converge down into panel 2. Do not add arrows between the three input tiles. Keep connectors clear of all text.
Main panels, exact text with line breaks:
1: heading "Choose a profile"; body "Select an agent" then "Harness, model," then "instructions". Vignette: farmer picking a profile card beside an agent figurine.
2: heading "Build launch bundle"; body "Agent Farm" then "assembles the setup". Vignette: open crate at a workbench.
3: heading "Write to repository"; body ".agent-farm/" then "generated/". Render these as two adjacent lines indicating the single path .agent-farm/generated/. Vignette: a project folder receiving a bundle.
4: heading "Open native terminal"; body "Claude Code or Codex" then "Work in your repository". Vignette: terminal hut with >_ symbol.
Three input tiles above panel 2, exact text:
"Shared skills" with smaller second line "References and assets";
"Child agents" with smaller second line "Agent definitions";
"Optional workspace" with smaller second line "MCP connections".
Important semantics: profile selects an agent definition specifying harness, model and instructions; that definition plus shared skills, child-agent definitions and optional workspace MCP connections feed the launch bundle; bundle is generated in target repo .agent-farm/generated/; the native Claude Code or Codex terminal opens to work in the chosen repo. This is configuration and launch, not autonomous plan-code-review progression.
Use large clean highly legible lettering, especially labels and path. Pixel-inspired headings but straightforward readable body type. Main flow in lower half, three inputs centered in upper half, no giant heading. The visual must remain readable at GitHub README width. Restrained pixel-art vegetation and tiny lanterns decorate panel bases; avoid scenery competing with the text. Exact spelling, no additional claims, no third-party logos, no watermark, no fake screenshot, no smooth 3D rendering.
